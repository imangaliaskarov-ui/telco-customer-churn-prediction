# telco-customer-churn-prediction
Проект по прогнозированию оттока клиентов телеком-компании (Telco Customer Churn) с использованием XGBoost. Достигнут F1-Score 0.6203 при Recall 81.02%.

# 🏆 Проект: Прогнозирование Оттока Клиентов (Telco Customer Churn)

### 1. Постановка Задачи и Бизнес-Ценность
* **Задача:** Бинарная классификация — предсказание, уйдет ли клиент (`Churn=Yes`).
* **Ценность:** Проактивное выявление рисковой группы для снижения стоимости удержания и повышения лояльности.
* **Метрика:** $\mathbf{F1-Score}$ (из-за сильного дисбаланса классов) и $\mathbf{Recall}$ (критически важен для бизнеса).

### 2. Данные и Первичная Обработка
* **Источник:** Датасет "Telco Customer Churn" (Kaggle).
* **Размер:** 7032 наблюдения, 20 признаков.
* **Пропуски:** 11 пропущенных значений в `TotalCharges` были **удалены** (менее 0.2% данных).
* **Выбросы:** Не удалялись, так как отражают реальное поведение премиальных клиентов.

### 3. Методология Предобработки (Feature Engineering)
Для подготовки данных к моделированию были выполнены следующие шаги:

1.  **Порядковое Кодирование (Ordinal Encoding):** Создан признак **`ContractType`** с порядковым кодированием (Month-to-month: 0, One year: 1, Two year: 2), отражающим шкалу лояльности.
2.  **Бинарное Кодирование:** Признаки 'Yes/No' и 'Male/Female' преобразованы в 1/0.
3.  **One-Hot Encoding (OHE):** Применен к остальным номинальным признакам (`InternetService`, `PaymentMethod` и т.д.) с использованием аргумента **`drop_first=True`** для предотвращения мультиколлинеарности.
4.  **Масштабирование:** Числовые признаки (`tenure`, `MonthlyCharges`, `TotalCharges`) масштабированы с помощью $\mathbf{StandardScaler}$.

### 4. Моделирование и Оптимизация
* **Baseline:** Протестированы Logistic Regression, Random Forest и XGBoost. XGBoost показал лучший начальный результат.
* **Оптимизация:** Применена $\mathbf{GridSearchCV}$ для тюнинга гиперпараметров $\mathbf{XGBoost}$ (лучшая модель).
* **Победившая Модель:** $\mathbf{XGBoost}$ (на робастном наборе признаков).

### 5. Ключевой Эксперимент (Доказательство Робастности)
**Цель:** Проверка критической значимости самого важного признака (`ContractType`).
* **Действие:** Признак `ContractType` был удален, и модель переобучена.
* **Результат:** **F1-Score вырос** с $\mathbf{0.6040}$ до $\mathbf{0.6203}$.
* **Вывод:** Модель $\mathbf{робастна}$, информация в `ContractType` является **избыточной** и компенсируется другими факторами. Финальная модель использует набор признаков без `ContractType`.

### 6. Финальные Метрики и Рекомендации

| Метрика | Значение | Комментарий |
| :--- | :--- | :--- |
| **F1-Score** | **0.6203** | Улучшенный результат после эксперимента. |
| **Recall (Полнота)** | **0.8102** | Из 100 уходящих клиентов модель найдет 81. **Готовность к внедрению.** |

**Ключевые факторы риска (по Feature Importance):**
1.  **tenure** (стаж)
2.  **InternetService_Fiber optic** (оптоволокно)
3.  **MonthlyCharges** (ежемесячный платеж)

**Рекомендации:**
1.  **Сфокусироваться на удержании** клиентов в "критической зоне" $\mathbf{0–15}$ месяцев стажа.
2.  Провести **технический аудит** услуги $\mathbf{Fiber\ optic}$ для устранения причин оттока.
3.  Модель готова к $\mathbf{проактивному}$ внедрению в $\mathbf{CRM}$ систему компании.

### 7. Требования для Воспроизведения

Для воспроизведения проекта необходимо использовать среду Python 3 и установить следующие библиотеки.

```bash
# Установка необходимых библиотек
pip install pandas numpy matplotlib seaborn scikit-learn xgboost

# Для запуска Jupyter Notebook
jupyter notebook
